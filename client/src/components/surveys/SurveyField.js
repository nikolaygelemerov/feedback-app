// SurveyField contains logic to render a single
// label and text input

import React, { memo } from 'react';

const SurveyField = () => {
  return <div>SurveyField</div>;
};

export default memo(SurveyField);
